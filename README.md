# Zend Framework DPR on Docker
How to run zend fw app on docker, please follow this step
## Installation
Already installed docker on machine
## Usage
This docker already running under compose, so run by following syntax:
```linux
docker-compose up -d
```